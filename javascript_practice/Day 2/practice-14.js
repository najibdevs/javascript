// Create a function that takes a name as an argument and returns a greeting message with that name.
function greeting(name) {
    let greeting = `Hello there ${name} it is great to meet you, the date today is 16/02/2024 
    and one day you will become a millioniare inshallah`
    return greeting
}

let results = greeting("Najib")
console.log(results)