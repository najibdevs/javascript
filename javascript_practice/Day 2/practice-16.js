// Create a function that converts minutes into seconds. The input will be an integer representing minutes.

function minutesToSeconds(number) {
    return number * 60
}

let results = minutesToSeconds(2)
console.log(results)